{
  "name": "mlistco-filter",
  "version": "1.5.1",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node scripts/build.mjs",
    "test": "node --test \"test/*.test.js\"",
    "check": "node scripts/verify-build.mjs && npm test"
  },
  "devDependencies": {
    "esbuild": "0.28.1",
    "jsdom": "26.1.0"
  }
}
